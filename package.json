{
  "name": "seance",
  "private": true,
  "scripts": {
    "build": "pnpm build:core && pnpm build:js:native",
    "build:core": "pnpm --filter @qult/seance build",
    "build:native": "pnpm --filter @qult/seance-native build",
    "build:demo": "pnpm --filter demo build",
    "dev:demo": "pnpm --filter demo dev",
    "publish:core": "pnpm --filter @qult/seance publish --access public",
    "publish:native": "pnpm --filter @qult/seance-native publish --access public"
  }
}
